name: Manual workflow

on:
  workflow_dispatch:
    inputs:
      name:
        description: 'Name'
        type: string
        required: true

jobs:
  manual: 
    runs-on: windows-latest

    steps:
     - name: Greetings
       run: echo "Hello ${{ inputs.name }}"
